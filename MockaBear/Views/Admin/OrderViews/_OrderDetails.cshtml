@* /Views/Admin/OrderViews/_OrderDetailModal.cshtml *@

<!-- ► MODAL : DÉTAILS DE LA COMMANDE ◄ -->
<div id="orderDetailModal" class="modal-overlay" style="display:none;">
    <div class="modal-content">
        <span class="close-btn" onclick="closeOrderDetailModal()">&times;</span>
        <h4>Order Details</h4>
        <table class="data-table">
            <thead>
                <tr>
                    <th>Product</th>
                    <th>Qty</th>
                    <th>Unit Price</th>
                    <th>Line Total</th>
                </tr>
            </thead>
            <tbody id="orderDetailBody">
                @* Rempli dynamiquement par JS *@
            </tbody>
        </table>
    </div>
</div>
